description: Invalid or expired token
content:
  application/json:
    schema:
      additionalProperties: false
      type: object
      required:
      - error
      - error_description
      properties:
        error:
          type: string
          description: Error type
        error_description:
          type: string
          description: Error description detail